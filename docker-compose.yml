version: '3.9'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build: ./frontend/chatbot-ui
    ports:
      - "80:80"
    depends_on:
      - backend
    volumes:
      - ./frontend/chatbot-ui/nginx.conf:/etc/nginx/nginx.conf:ro